name: action
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "building..."
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test
        run: echo "testing..."
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: deploy
        run: echo "deploying..."
