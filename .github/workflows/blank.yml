name: action
on: workflow_dispatch
jobs:
  task-1:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "testing"
          date
          hostname
          free -m
          df -h
